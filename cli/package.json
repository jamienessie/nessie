{
  "name": "nessie-cli",
  "version": "0.3.1",
  "description": "Nessie CLI — single-operator AI company OS (fork of Paperclip)",
  "type": "module",
  "bin": {
    "nessie": "./dist/index.js"
  },
  "keywords": [
    "nessie",
    "paperclip",
    "ai",
    "agents",
    "orchestration",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/paperclipai/paperclip",
    "directory": "cli"
  },
  "homepage": "https://github.com/paperclipai/paperclip",
  "bugs": {
    "url": "https://github.com/paperclipai/paperclip/issues"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "node --input-type=module -e \"import esbuild from 'esbuild'; import config from './esbuild.config.mjs'; await esbuild.build(config);\" && chmod +x dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "@nessie/adapter-acpx-local": "workspace:*",
    "@nessie/adapter-claude-local": "workspace:*",
    "@nessie/adapter-codex-local": "workspace:*",
    "@nessie/adapter-cursor-local": "workspace:*",
    "@nessie/adapter-gemini-local": "workspace:*",
    "@nessie/adapter-opencode-local": "workspace:*",
    "@nessie/adapter-pi-local": "workspace:*",
    "@nessie/adapter-openclaw-gateway": "workspace:*",
    "@nessie/adapter-utils": "workspace:*",
    "@nessie/db": "workspace:*",
    "@nessie/server": "workspace:*",
    "@nessie/shared": "workspace:*",
    "drizzle-orm": "0.38.4",
    "dotenv": "^17.0.1",
    "commander": "^13.1.0",
    "embedded-postgres": "^18.1.0-beta.16",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
